[package]
name = "dev_sweep_lint"
version = "0.1.0"
edition = "2024"

[dependencies]
slint = "1.15.1"
i-slint-backend-winit = "1.15.1" # Must be the SAME version

[build-dependencies]
slint-build = "1.15.1"
